import { describe, expect, it } from 'vitest';

/**
 * Placeholder proving the Vitest wiring works end to end.
 * Delete it once the first real module (pattern codec / notation engine) has tests.
 */
describe('toolchain', () => {
  it('runs tests', () => {
    expect(1 + 1).toBe(2);
  });
});
